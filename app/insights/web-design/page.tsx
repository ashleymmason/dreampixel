import Link from "next/link"
import Image from "next/image"
import { insights } from "@/lib/insights"
export const metadata = { title: "Web Design Insights", description: "Practical web design thinking from Dream Pixel." }
export default function WebDesignInsightsPage() { const items = insights.filter((item) => item.category === "Web Design"); return <div className="bg-background"><section className="section-dark px-6 py-24 md:px-12 md:py-32"><div className="mx-auto max-w-7xl"><p className="eyebrow text-accent">INSIGHTS / WEB DESIGN</p><h1 className="mt-8 text-6xl font-semibold tracking-tight md:text-8xl">Web design ideas.</h1><p className="mt-8 max-w-xl text-lg leading-relaxed text-muted-foreground">Thoughtful approaches to making websites clearer, more useful and more like the businesses behind them.</p></div></section><main className="mx-auto max-w-7xl px-6 py-20 md:px-12"><div className="grid gap-16 md:grid-cols-2">{items.map((item) => <Link key={item.slug} href={`/insights/${item.slug}`} className="group"><div className="relative aspect-[16/10] overflow-hidden"><Image src={item.image} alt={item.alt} fill className="object-cover transition-transform duration-500 group-hover:scale-105" sizes="50vw" /></div><p className="eyebrow mt-6 text-muted-foreground">{item.date} / {item.readTime}</p><h2 className="mt-3 text-3xl font-semibold tracking-tight group-hover:text-primary md:text-4xl">{item.title}</h2><p className="mt-4 leading-relaxed text-muted-foreground">{item.excerpt}</p></Link>)}</div></main></div> }
