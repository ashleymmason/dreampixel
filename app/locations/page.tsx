import type { Metadata } from "next"
import Link from "next/link"
import Image from "next/image"
import { ArrowUpRight } from "lucide-react"
import { locations } from "@/lib/locations"

export const metadata: Metadata = { title: "Web Design Locations in Devon | Dream Pixel", description: "Dream Pixel designs and builds high-performance websites for ambitious businesses in Barnstaple, Bideford, Exeter, Plymouth, Torquay and across Devon.", alternates: { canonical: "/locations" } }

export default function LocationsPage() {
  return <main className="overflow-hidden bg-background"><section className="border-b border-border px-6 py-24 md:px-12 md:py-36"><div className="container"><p className="mb-7 font-mono text-xs uppercase tracking-[.3em] text-accent">Web design / Devon</p><h1 className="max-w-6xl font-display text-6xl font-black leading-[.86] tracking-[-.07em] md:text-8xl lg:text-[9.5rem]">Good websites<br /><span className="text-primary">are local too.</span></h1><div className="mt-12 grid gap-8 border-t border-border pt-6 md:grid-cols-[1fr_.8fr] md:items-end"><p className="max-w-2xl text-lg leading-relaxed text-muted-foreground md:text-xl">Dream Pixel is a digital studio based in Devon, working with businesses across the county and beyond. Wherever you are, the goal is the same: a website that explains your value and makes the next step obvious.</p><Link href="/contact" className="link-arrow text-sm font-semibold">Talk through your project <ArrowUpRight className="size-4" /></Link></div></div></section><section className="bg-card px-6 py-20 md:px-12 md:py-28"><div className="container"><div className="mb-12 max-w-2xl"><p className="font-mono text-xs uppercase tracking-[.3em] text-primary">Areas we serve</p><h2 className="mt-5 font-display text-5xl font-black leading-[.9] tracking-[-.06em] md:text-7xl">Local knowledge.<br />Work that travels.</h2></div><div className="grid gap-px bg-border md:grid-cols-2">{locations.map((location) => <Link href={`/web-design-${location.slug}`} key={location.slug} className="group grid gap-6 bg-card p-5 transition hover:bg-primary hover:text-primary-foreground sm:grid-cols-[.75fr_1fr] sm:items-end"><div className="relative aspect-[4/3] overflow-hidden"><Image src={location.image} alt={location.imageAlt} fill className="object-cover transition duration-500 group-hover:scale-105" sizes="(min-width: 768px) 35vw, 90vw" /></div><div><p className="font-mono text-xs uppercase tracking-[.25em] text-accent group-hover:text-primary-foreground">{location.region}</p><h3 className="mt-4 font-display text-4xl font-black tracking-[-.05em]">{location.name}</h3><p className="mt-4 text-sm leading-relaxed text-muted-foreground group-hover:text-primary-foreground/80">{location.descriptor}</p><span className="mt-8 flex items-center gap-2 text-sm font-semibold">Explore {location.name} <ArrowUpRight className="size-4 transition group-hover:translate-x-1 group-hover:-translate-y-1" /></span></div></Link>)}</div></div></section><section className="px-6 py-20 md:px-12 md:py-28"><div className="container grid gap-10 lg:grid-cols-[.7fr_1.3fr]"><div><p className="font-mono text-xs uppercase tracking-[.3em] text-accent">Beyond the map</p><h2 className="mt-5 font-display text-5xl font-black leading-[.9] tracking-[-.06em] md:text-7xl">Not in the list?<br /><span className="text-primary">That&apos;s fine.</span></h2></div><div><p className="max-w-2xl text-lg leading-relaxed text-muted-foreground">We work remotely and collaboratively, so the right fit is not limited by a postcode. If you are building something ambitious elsewhere in the UK, we would still like to hear about it.</p><Link href="/contact" className="mt-8 inline-flex items-center gap-3 border-b border-foreground pb-3 text-sm font-semibold uppercase tracking-[.16em] hover:text-primary">Start a conversation <ArrowUpRight className="size-4" /></Link></div></div></section></main>
}
